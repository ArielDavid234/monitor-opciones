# ── OptionsKing Analytics — Dependencies ──────────────
# Pinned to compatible ranges (minor version) for stability.
# Update periodically with: pip install --upgrade -r requirements.txt

# Framework
streamlit>=1.54,<2
streamlit-aggrid>=0.3.4

# Data & Finance
yfinance>=1.1,<2
pandas>=2.2,<3
numpy>=2.1,<3
scipy>=1.14,<2

# HTTP / Scraping
curl_cffi>=0.13,<1
requests>=2.32,<3
beautifulsoup4>=4.12,<5
feedparser>=6.0,<7

# Reports & Visualization
python-docx>=1.1,<2
plotly>=6.0,<7
kaleido>=0.2